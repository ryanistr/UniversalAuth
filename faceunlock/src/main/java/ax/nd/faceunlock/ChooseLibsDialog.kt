package ax.nd.faceunlock

import android.content.Context

// Logic removed as libraries are now bundled in the APK.
class ChooseLibsDialog(context: Context, private val viewModel: ChooseLibsViewModel) {
    fun open() {
        // No-op
    }
}