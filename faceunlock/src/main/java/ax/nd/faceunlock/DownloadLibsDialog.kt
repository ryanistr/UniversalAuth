package ax.nd.faceunlock

import android.app.Activity

// Logic removed as libraries are now bundled in the APK.
class DownloadLibsDialog(private val activity: Activity, private val viewModel: ChooseLibsViewModel) {
    fun open() {
        // No-op
    }
}